# HTTP Reverse Proxy

This deployable web application will redirect HTTP requests to the specified target.

This is an extension of [Smiley's HTTP Proxy Servlet](https://github.com/mitre/HTTP-Proxy-Servlet)

All of the servlet init parameters can be specified as system properties, starting with "proxy.".

